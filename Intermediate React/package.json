{
  "name": "intermediate-react",
  "version": "1.0.0",
  "description": "- In traditional JS, when we want to render a list of items into the DOM, we have to write the HTML as strings, then find a target DOMnode to inject the items",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react-app": "^3.1.2"
  }
}
